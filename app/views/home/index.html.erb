<div class="flex flex-col items-center justify-center h-screen bg-gray-100">
  <div class="bg-white p-8 rounded shadow-md text-center">
    <h2 class="text-2xl font-bold mb-4">You haven't been verified yet. Stay tuned!</h2>
    <p class="text-gray-600 mb-8">We are processing your verification. Please be patient.</p>
    <% if user_signed_in? %>
      <p>Current User Role: <%= current_user.role %></p>
    <% else %>
      <p>No user signed in</p>
    <% end %>

    <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
  </div>
</div>
