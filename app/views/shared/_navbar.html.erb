<nav class="bg-black p-4">
  <div class="container mx-auto flex justify-between items-center">
    <div class="text-white font-bold text-xl">Course Registration</div>
    <div class="flex items-center space-x-4">
      <% if user_signed_in? %>
        <%= link_to 'Sign Out', destroy_user_session_path, data: {"turbo-method": :delete}, class: 'text-white' %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path, class: 'text-white' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'text-white' %>
      <% end %>
    </div>
  </div>
</nav>
